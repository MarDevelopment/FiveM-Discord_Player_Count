const Discord = require("discord.js");
const bot = new Discord.Client();

module.exports.run = async (bot, message, args) => {
    //HER SKAL DIN KOMMANDO VÆRE
}

module.exports.help = {
    name: "" //NAVNET ER LIG MED KOMMANDOEN
}